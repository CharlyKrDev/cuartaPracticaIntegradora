<h2>Perfil de Usuario</h2>
<main class="containerCurrent">

    <aside class="btnCurrent">
        {{#if (or roleUser rolePremium)}}
        <button style="margin-top:30px"><a href="https://proyectofinalbackend-2024.up.railway.app/messages/">Atención por chat</a></button>
        <button><a href="https://proyectofinalbackend-2024.up.railway.app/carts/cart/{{user.cart}}">Ver mi carrito</a></button>
        {{/if}}
        {{#if (or roleAdmin rolePremium)}}
        <button><a href="https://proyectofinalbackend-2024.up.railway.app/dashBoardProducts">Panel de administrador</a></button>
        {{/if}}


        <button><a href="https://proyectofinalbackend-2024.up.railway.app/products">Inicio</a></button>

        <form action="/sessions/logout" method="POST">
            <button type="submit">Cerrar Sesión</button>
        </form>
    </aside>

    <section>
        <p>Nombre: {{user.first_name}}</p>
        <p>Apellido: {{user.last_name}}</p>
        <p>Email: {{user.email}}</p>
        <p>Edad: {{user.age}}</p>
        <p>Mi rol: {{user.role}}</p>
        {{#if (or roleUser rolePremium)}}
        <p>Carrito: <span style="font-weight: bold; color: green;">Activo</span></p>
        {{else}}
        <p>Carrito: <span style="font-weight: bold; color: red;">Desactivado</span></p>
        {{/if}}

    </section>
</main>